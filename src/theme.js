import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
  colors: {
    brand: {
      primary: "#D4A373",
      secondary: "#A78A7F",
      text: "#5C4B43",
      background: "#FFF9F2",
      accent: "#E6B8A2",
      success: "#D4A373",
    }
  },
});

export default theme; 